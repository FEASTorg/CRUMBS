# CRUMBS Documentation

CRUMBS is a C99-based protocol library with thin platform HALs (Arduino, Linux). It provides variable-length messaging (4–31 bytes) with automatic serialization and CRC-8 validation.

For complete documentation, see [index.md](index.md).
