[env:nanoatmega328]
platform = atmelavr
board = nanoatmega328
framework = arduino
; allow the example project to resolve the library from the repo root during local development
[platformio]
default_envs = nanoatmega328

lib_extra_dirs = ${PROJECT_DIR}/../../..

; (Removed duplicated section) The example targets the Nano; use nanoatmega328 via default_envs above.
